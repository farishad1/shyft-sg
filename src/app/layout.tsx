import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Shyft Sg | Unlock Shifts at Unique Stays",
  description: "Singapore's hospitality staffing platform connecting verified workers with boutique hotels, capsule hotels, and serviced apartments. Find flexible work or hire staff today.",
  keywords: ["hospitality jobs", "hotel staffing", "Singapore jobs", "flexible work", "boutique hotels"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
